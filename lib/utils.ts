/**
 * Utility functions for Audit Evidence Review System (AERS)
 */

/**
 * Example utility function to combine CSS class names helper
 */
export function cn(...classes: (string | undefined | null | boolean)[]) {
  return classes.filter(Boolean).join(' ');
}
